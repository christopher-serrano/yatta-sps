<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/searchFormFragment">

    <fragment
        android:id="@+id/searchFormFragment"
        android:name="yatta.serranocjm.sps.ui.fragment.SearchFormFragment"
        android:label="fragment_search_form"
        tools:layout="@layout/fragment_search_form" >
        <action
            android:id="@+id/action_searchFormFragment_to_searchResultFragment"
            app:destination="@id/searchResultFragment" />
    </fragment>
    <fragment
        android:id="@+id/searchResultFragment"
        android:name="yatta.serranocjm.sps.ui.fragment.SearchResultFragment"
        android:label="fragment_search_result"
        tools:layout="@layout/fragment_search_result" />
</navigation>